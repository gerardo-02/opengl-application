#ifndef MATRIX_UTILS_H
#define MATRIX_UTILS_H

/*void multiply_matrix(float m1[][4], float m2[][4], float result[][4]){
for (int i=0; i<4; i++)
    for (int j=0; j<4; j++)
        for (int k=0; k<4; k++){
            result[i][j] += m1[i][k] * m2[k][j];
        }
}*/

#endif // MATRIX_UTILS_H
